Q1. Which of the following is the primary purpose of embeddings in a Retrieval-Augmented
Generation (RAG) pipeline?
A1. Embeddings convert text into numerical vectors so the system can find semantically similar content when retrieving relevant information for the model.



Q2. Which HTTP method is most appropriate for creating a new resource through a REST API?
A2. POST 



Q3. In React, which concept is primarily used to manage component-level dynamic data?
A3. State Management 



Q4. What is one major advantage of using FastAPI or Express.js for backend services?
A4. Both FastAPI and Express.js are lightweight and developer-friendly, letting you spin up fast, scalable backend APIs without a ton of boilerplate code.



Q5. In a Gen-AI application, hallucination refers to:
A5. Hallucination is when an AI confidently generates information that seems correct but is fabricated and factually wrong


